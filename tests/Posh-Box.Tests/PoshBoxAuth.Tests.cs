using Xunit;
using Moq;
using PoshBox;

namespace PoshBox.Tests
{
    public class PoshBoxAuthTests
    {
        [Fact(Skip = "Not Implemented")]
        public void LoadJWTAuthConfigShouldLoadConfigWithoutError()
        {
            //var config = "..\\assets\\config.txt";

            // Turns out mocking static classes is hard. But I'll figure something out later.

            //PoshBoxAuth.LoadJWTAuthConfig(config);

        }
    }
}
